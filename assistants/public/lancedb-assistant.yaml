name: LanceDBAssistant
version: 0.0.1
schema: v1

rules:
  - uses: lancedb/lancedb-rules

docs:
  - uses: starter/lancedb-enterprise-docs
  - uses: starter/lancedb-open-source-docs

prompts:
  - uses: starter/lancedb-table-prompt

models:
  - uses: ollama/gemma3-27b

  - uses: ollama/qwen2.5-coder-1.5b
    override:
      roles:
        - autocomplete

  - uses: ollama/nomic-embed-text-latest

context:
  - uses: continuedev/diff-context
  - uses: continuedev/codebase-context
  - uses: continuedev/url-context
  - uses: continuedev/folder-context
  - uses: continuedev/terminal-context
  - uses: continuedev/code-context
  - uses: continuedev/file-context
